<form class="form-inline">
	<fieldset>
		<legend class="question-title">{{question}}</legend>

		<div class="fields">
			{{#text}}
				{{#answers}}
				<div class="control-group row-fluid">
					<label class="question-field control-label span5" for="{{name}}">
						{{display}}
					</label>
					<input id="{{name}}" class="question-input span7" name="{{name}}">
				</div>
				{{/answers}}
			{{/text}}

			{{#radio}}
			{{/radio}}
		</div>

		<button class="btn btn-large" type="submit">Next</button>
	</fieldset>
</form>
